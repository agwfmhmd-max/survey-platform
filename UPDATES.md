# تحديث منصة الاستبيان الأكاديمية

## النتيجة

تم تحسين المنصة لتبدو كمنصة بحث جامعي احترافية حول التأمين البارامتري والمخاطر المناخية في موريتانيا، مع الحفاظ على Supabase ونظام المشرف وإرسال الإجابات والنتائج المجمعة.

## التحسينات الجديدة

| المجال | ما تم تنفيذه |
| --- | --- |
| الهوية البصرية | تم فصل الهوية إلى شعارين مستقلين: شعار المعهد الرسمي المرفق يظهر في الرأس والتذييل، وشعار فكرة المشروع المتجهي يظهر في بطاقة المشروع داخل القسم الرئيسي. لا يتم دمج الشعارين أو تركيب أحدهما فوق الآخر. |
| التذييل | تذييل احترافي يعرض هوية المعهد بوضوح وعبارة **Développé par MDA** داخل شارة مستقلة. |
| أعضاء الفريق | قسم عام جذاب لأعضاء الفريق النشطين، مع الاسم والدور والصورة. أضيفت لوحة مشرف لإضافة العضو وتعديله وتعطيله وترتيبه. |
| الصور | يمكن للمشرف إدخال رابط صورة أو رفع صورة إلى bucket باسم `team-members` في Supabase Storage. |
| منع إعادة الإرسال | بعد الإرسال الناجح تظهر رسالة شكر واضحة وتُقفل شاشة بدء الاستبيان. وعند اكتشاف رد سابق من Supabase أو من `localStorage` تظهر رسالة واضحة بأن المشاركة مسموحة مرة واحدة فقط. |
| الأسئلة المكررة | قبل الحفظ يتم فحص تكرار السؤال بالعربية أو الفرنسية، وفحص تكرار نصوص الخيارات وقيمها. كما تعرض لوحة المشرف تحذيرًا وعلامة على الأسئلة المكررة الموجودة مسبقًا. |
| حذف الأسئلة | أصبح زر الحذف ينفذ حذفًا نهائيًا للسؤال وخياراته وإجاباته المرتبطة بعد تأكيد صريح من المشرف، مع استخدام `ON DELETE CASCADE` لحل تعارض المفتاح الخارجي. أما أعضاء الفريق فيستمر تعطيلهم منطقيًا حتى يمكن التحكم في ظهورهم العام. |
| إصلاح Supabase | يضيف SQL عمود `image_url` إذا كان ناقصًا، ويعيد إنشاء سياسات الفريق بأمان، ويجعل العلاقة بين الأسئلة والإجابات والخيارات قابلة للحذف المتسلسل دون حذف البيانات عند تشغيل SQL. |
| النصوص المرئية | أزيلت علامات الاستفهام العربية والفرنسية من نصوص الأسئلة عند العرض والحفظ الجديد، دون المساس بعوامل JavaScript الشرطية أو روابط الطقس التقنية. |

## الملفات المعدلة أو المضافة

| الملف | الوصف |
| --- | --- |
| `src/SondageAssuranceParametrique.jsx` | دمج الشعار، الفريق، التذييل، منع الإرسال المتكرر، التحقق من التكرار، إدارة الفريق، رفع الصور، وتحسين رسائل الحالات. |
| `src/components/TeamSection.jsx` | مكوّن بطاقات أعضاء الفريق للعرض العام. |
| `src/index.css` | أنماط الشعار والتذييل وبطاقات الفريق ولوحة إدارة الصور والتحذيرات والاستجابة للهاتف. |
| `public/iscae-official-logo.jpeg` | شعار المعهد الرسمي المرفق، ويُستخدم فقط في هوية المعهد والرأس والتذييل. |
| `public/iscae-parametric-logo.svg` | شعار فكرة المشروع المتجهي، ويُستخدم فقط في بطاقة التأمين البارامتري والمخاطر المناخية. |
| `TEAM_MEMBERS.sql` | SQL نظيف وآمن لترقية جدول الفريق، إضافة `image_url`، إعداد الصور، وإصلاح الحذف المتسلسل للأسئلة والإجابات والخيارات. |
| `UPDATES.md` | وثيقة التسليم الحالية. |

## إعداد Supabase المطلوب لأعضاء الفريق

نفّذ ملف `TEAM_MEMBERS.sql` مرة واحدة في مشروع Supabase الحالي بصلاحية مالك المشروع. الملف قابل لإعادة التشغيل، لا يحذف أي جدول أو سجل عند تشغيله، ويعتمد على جدول `admins` الحالي وعلى أن المشرف المسجّل موجود في `admins.id`. بعد تشغيله، يعالج الحذف النهائي من لوحة المشرف عبر `ON DELETE CASCADE` عند تنفيذ الحذف لاحقًا.

بعد التنفيذ سيستطيع المشرف إضافة بيانات الأعضاء من تبويب **Équipe / الفريق**، ورفع الصور إلى bucket `team-members`. تظهر الأعضاء للعامة على بطاقات حديثة مع صورة ودور وترتيب. إذا لم يتم تنفيذ الملف، ستبقى المنصة تعمل، لكن قسم الفريق لن يظهر ولن تتوفر وظائف إدارة الأعضاء.

## إعداد منع الإجابة الثانية

يستخدم التطبيق مفتاحًا محليًا باسم `survey_submitted_assurance-parametrique-2026` لمنع المستخدم من العودة إلى شاشة البدء في الجهاز نفسه. كما يستمر استخدام `respondent_id` وRPC `submit_survey_response` أو القيود الحالية في Supabase لمنع التكرار على مستوى قاعدة البيانات. مسح بيانات المتصفح ليس آلية حماية يمكن الاعتماد عليها وحدها؛ لذلك يجب الإبقاء على حماية Supabase/RPC مفعلة.

## التحقق النهائي

تم تشغيل `npm run build` بنجاح بعد التعديلات. كما تم فحص إصلاح `image_url`، والحذف المتسلسل، ومنع الردود المكررة، ورسائل الشكر والمشاركة الواحدة، وتنظيف علامات الاستفهام المرئية. فحص البحث النصي لم يجد علامة استفهام مرئية مستقلة في الواجهة؛ الموجود فقط معاملات JavaScript وروابط تقنية.

## التشغيل

```bash
cp .env.example .env
# أضف VITE_SUPABASE_URL و VITE_SUPABASE_ANON_KEY
npm install
npm run dev
```

---

## تحديث 2 — التوافق الكامل مع الهاتف ولوحة إحصائيات المشاركين

هذا التحديث لا يحذف ولا يعدّل أي وظيفة سابقة (الأسئلة، الإرسال، منع التكرار، إدارة الفريق، الطقس، الاتصال بـ Supabase). التعديلات إضافية بالكامل.

### 1) لوحة إحصائيات المشاركين (تبويب جديد "Statistiques" — أول تبويب في لوحة المشرف)

- بطاقة كبيرة **Participants ayant terminé** تعرض إجمالي المشاركين الذين أكملوا الاستبيان فعليًا (تُستخدم نفس قيمة `public_survey_participant_counts` الموجودة مسبقًا، دون أي استعلام إضافي أو بيانات وهمية).
- بطاقات: **Aujourd'hui**، **Cette semaine**، **Moyenne quotidienne**، **Meilleur jour**.
- أزرار فلترة حسب المدة: Aujourd'hui / Hier / 7 derniers jours / 30 derniers jours / Toutes les réponses، بالإضافة إلى **Sélectionner une date** (Date Picker) لعرض تفاصيل يوم واحد بدقة الساعة.
- ثلاثة رسوم بيانية SVG خفيفة (بدون مكتبة خارجية جديدة — نفس أسلوب `ResultChart` الموجود مسبقًا): **Participants complétés par jour**، **Participants complétés par heure**، **Évolution des réponses**. تتجاوب مع حجم الشاشة ولا تُخرج الصفحة عن حدودها (تمرير أفقي داخل الرسم نفسه فقط عند الحاجة، وليس للصفحة كاملة).
- **معالجة صحيحة للتوقيت**: التحويل من UTC (كما تُخزَّن في Supabase) إلى توقيت `Africa/Nouakchott` عبر `Intl.DateTimeFormat`، بدل استخدام توقيت ثابت خاطئ.
- حالات تحميل (`Chargement des statistiques…`)، وحالة عدم وجود بيانات (`Aucune réponse complétée pour cette période.`)، وحالة خطأ — بالعربية والفرنسية.
- **مصدر البيانات**: يقرأ التطبيق فقط `survey_responses.created_at` (بدون أي معلومة شخصية) للاستبيان الحالي، ويُجري كل التجميع (يومي/ساعي) في المتصفح — طلب واحد خفيف فقط عند فتح تبويب الإحصائيات، وليس عند كل تحميل للوحة.

**ملف SQL جديد وآمن: `STATS_DASHBOARD_ACCESS.sql`** — يجب تشغيله مرة واحدة في Supabase (بصلاحية مالك المشروع) لمنح المشرف صلاحية قراءة `created_at` من جدول `survey_responses`. الملف قابل لإعادة التشغيل، **لا يحذف أي سياسة أو جدول موجود، ولا يغيّر حالة RLS الحالية** — يضيف فقط سياسة قراءة إضافية للمشرفين، تبقى بلا أثر إن كانت القراءة متاحة أصلاً.

### 2) التوافق الكامل مع الهاتف والأجهزة اللوحية

- لوحة المشرف: تبويبات (`Statistiques`، `Questions`، `Équipe`، `Résultats`، `Météo`) أصبحت قابلة للتمرير الأفقي بسلاسة على الشاشات الضيقة بدل أن تتكسر أو تخرج عن الشاشة.
- بطاقات الإحصائيات الرئيسية أصبحت مرنة (`auto-fit`) وتتموضع تلقائيًا حسب عرض الشاشة، بدل عدد أعمدة ثابت.
- إضافة نقاط توقف (breakpoints) عند 900px و640px و380px لضبط الشبكات، الأزرار، وحقول الإدخال على الهواتف الصغيرة جدًا (320px) والمتوسطة والكبيرة والأجهزة اللوحية.
- التأكد من عدم وجود Horizontal Overflow على مستوى الصفحة: أي محتوى عريض (الرسوم البيانية) يُمرَّر أفقيًا داخل حاويته فقط.
- دعم RTL/LTR بشكل صحيح عبر CSS logical properties (`inset-inline-end`، `margin-inline-end`) بدل `right/left` الثابتة التي كانت تكسر الاتجاه عند اللغة العربية (تم إصلاح مؤشر حالة عضو الفريق وتأثير visual-pulse في الصفحة الرئيسية).
- دعم `prefers-reduced-motion` لتعطيل الحركات لمن يفضّل ذلك.

### 3) إعادة تصميم قسم أعضاء الفريق (Team Section)

- كل عضو الآن داخل بطاقة عرض أكاديمي: رقم ترتيب (01, 02…)، صورة دائرية أكبر، تأثير Hover هادئ على الحاسوب، ودخول تدريجي خفيف (Fade/Slide) عند ظهور القسم لأول مرة على الشاشة (`IntersectionObserver`، بدون أي تأثير على الأداء أو على من يفعّل تقليل الحركة).
- الشبكة: 3 أعضاء في الصف على الحاسوب (4 عند وجود 4 أعضاء فأكثر على الشاشات الكبيرة جدًا)، عمودان على الأجهزة اللوحية، عضو واحد في كل صف على الهاتف.

### الملفات المعدّلة أو المضافة في هذا التحديث

| الملف | التغيير |
| --- | --- |
| `src/SondageAssuranceParametrique.jsx` | إضافة تبويب ومكوّن الإحصائيات (`ParticipantStatsPanel`, `TimeSeriesChart`)، دوال مساعدة للتوقيت المحلي، تبويبات المشرف قابلة للتمرير، دون تعديل أي منطق سابق (الأسئلة، الإرسال، الفريق، الطقس، المصادقة). |
| `src/components/TeamSection.jsx` | إعادة تصميم بصري كامل (لا تغيير في البيانات المستهلكة من Supabase). |
| `src/index.css` | تحسينات Responsive شاملة، أنماط الإحصائيات الجديدة، إصلاحات RTL، `prefers-reduced-motion`. |
| `STATS_DASHBOARD_ACCESS.sql` | صلاحية قراءة إضافية آمنة وقابلة لإعادة التشغيل لجدول `survey_responses`. |

### التحقق

تم تشغيل `npm install` و`npm run build` بنجاح بعد كل التعديلات دون أي خطأ Build. لم يُحذف أو يُعدَّل أي مسار Supabase (URL، مفاتيح، جداول، RLS الحالية، RPC `submit_survey_response`)، ولم تُحذف أو تُغيَّر أي وظيفة أو نص أو ترتيب سؤال سابق.
